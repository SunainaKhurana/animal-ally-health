
// This component is no longer needed as we've moved to a unified list view
// All processing and completed reports are now shown in a single list with manual AI analysis triggers

export default function ProcessingReportsSection() {
  return null;
}
